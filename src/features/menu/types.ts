export type MenuItem = {
  id: string;
  name: string;
  tagline: string;
  description: string;
  ingredients: string;
  allergens: string[];
  price: string;
  unit: string;
  photoUrl: string | null;
};
